<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.localite">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ConsumerLogIn"></activity>
        <activity
            android:name=".Pro_list"
            android:parentActivityName=".Consumer_frontpage" />
        <activity android:name=".Provider_log_in" />
        <activity android:name=".Consumer_frontpage" />
        <activity
            android:name=".ProviderSignIn"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ConsumerSignIn"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>